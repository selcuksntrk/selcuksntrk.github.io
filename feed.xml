<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://selcuks.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://selcuks.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-03T13:08:15+00:00</updated><id>https://selcuks.com/feed.xml</id><title type="html">blank</title><subtitle>Selcuk Senturk&apos;s AI/ML engineering portfolio and blog, sharing work on AI agents, RAG systems, computer vision, LLM applications, and production-ready machine learning. </subtitle><author><name>Selcuk Senturk</name></author><entry><title type="html">Introduction to Machine Learning</title><link href="https://selcuks.com/blog/2024/introduction-to-machine-learning/" rel="alternate" type="text/html" title="Introduction to Machine Learning"/><published>2024-10-13T09:00:00+00:00</published><updated>2024-10-13T09:00:00+00:00</updated><id>https://selcuks.com/blog/2024/introduction-to-machine-learning</id><content type="html" xml:base="https://selcuks.com/blog/2024/introduction-to-machine-learning/"><![CDATA[<p>Machine learning is usually introduced as a list of algorithms. That is the wrong place to start. Algorithms are easy to look up. What is harder, and what actually decides whether a project works, is knowing what kind of problem you have, what kind of system fits it, and what tends to go wrong along the way.</p> <p>This post covers that foundation. It is the first in a series that builds toward designing and training models for real problems.</p> <blockquote class="block-tip"> <p><strong>The Three Questions</strong></p> <p>How is the system taught, when is it taught, and how does it generalize to things it has not seen? Everything below is an answer to one of those three.</p> </blockquote> <hr/> <h2 id="types-of-machine-learning">Types of Machine Learning</h2> <p>There are three main categories of machine learning systems:</p> <ul> <li><strong>Supervision modes</strong>: supervised, unsupervised, and reinforcement learning</li> <li><strong>Learning modes</strong>: online learning and batch learning</li> <li><strong>Inference types</strong>: instance-based and model-based learning</li> </ul> <p>These categories are not mutually exclusive and can be combined according to the needs of the problem.</p> <pre><code class="language-mermaid">flowchart TB
  ML["Machine learning system"]
  ML --&gt; S["Supervision mode&lt;br/&gt;how it is taught"]
  ML --&gt; L["Learning mode&lt;br/&gt;when it is taught"]
  ML --&gt; I["Inference type&lt;br/&gt;how it generalizes"]
  S --&gt; S1["Supervised"]
  S --&gt; S2["Unsupervised"]
  S --&gt; S3["Reinforcement"]
  L --&gt; L1["Batch / offline"]
  L --&gt; L2["Online / incremental"]
  I --&gt; I1["Instance-based"]
  I --&gt; I2["Model-based"]
</code></pre> <p>The three axes are independent. You pick one option from each, and the combination describes the system far better than the name of the algorithm does.</p> <h3 id="example">Example</h3> <p>A self-driving car might use a machine learning system that employs supervised learning, learns through batch learning, and reasons through a statistical model that enables short-term predictions, such as detecting that the car ahead is braking and the system needs to activate the brakes soon.</p> <p>Placed on the three axes, that system is:</p> <table> <thead> <tr> <th>Axis</th> <th>Choice</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td>Supervision</td> <td>Supervised</td> <td>Training clips are labeled with “braking” or “not braking”</td> </tr> <tr> <td>Learning mode</td> <td>Batch</td> <td>Retraining a driving model in production, on the fly, would be unsafe</td> </tr> <tr> <td>Inference type</td> <td>Model-based</td> <td>The car cannot carry its entire training set and search it at 120 km/h</td> </tr> </tbody> </table> <hr/> <h2 id="supervised-learning">Supervised Learning</h2> <p>The most common learning mode is supervised learning. This means training the system with data that contains the desired solution. These solutions are called <strong>labels</strong>.</p> <p>An example of labeled data could be an image of a car, which is the input data, and its assigned car model, which is the label. After a machine learning system is trained on data and labels, it can take a new image and output its predicted label. The metrics used to measure the system’s ability to assign correct labels vary by problem, and they are just as important as the training process itself.</p> <p>In code, this shape is always the same: a matrix of inputs, a vector of labels, and a <code class="language-plaintext highlighter-rouge">fit</code> call.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LogisticRegression</span>

<span class="c1"># X_train: one row per sample, one column per feature
# y_train: the label we want the model to learn to predict
</span><span class="n">model</span> <span class="o">=</span> <span class="nc">LogisticRegression</span><span class="p">()</span>
<span class="n">model</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="n">predictions</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
</code></pre></div></div> <p>The two most common tasks addressed with supervised learning are <strong>classification</strong> and <strong>regression</strong>.</p> <h3 id="classification">Classification</h3> <p>A classification problem assigns a discrete label to a new input. This can be a numerical value or a category, such as deciding whether a given image represents a dog or a cat. In either case, the answer belongs to a well-defined set of options. Properly defining those options can significantly affect model performance.</p> <p>Training a classifier means minimizing a loss that punishes confident wrong answers. The usual choice is cross-entropy:</p> \[L = -\frac{1}{n} \sum_{i=1}^{n} \sum_{c=1}^{C} y_{i,c} \log(\hat{y}_{i,c})\] <p>Here \(y_{i,c}\) is 1 when sample \(i\) truly belongs to class \(c\) and 0 otherwise, and \(\hat{y}_{i,c}\) is the probability the model assigned to that class. Notice what the logarithm does: being confidently wrong costs far more than being uncertain.</p> <h3 id="regression">Regression</h3> <p>A regression problem assigns a continuous value to new data. For example, a model might predict the price of a house from features such as the number of rooms, size in square meters, and number of bathrooms. In this case, the output belongs to a continuous range rather than a finite set.</p> <p>The corresponding loss measures distance rather than surprise. Mean squared error is the standard starting point:</p> \[\text{MSE} = \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2\] <p>Squaring makes large mistakes dominate the total, which is useful when big errors are genuinely worse, and harmful when your data contains outliers you do not care about. Mean absolute error is the common alternative when outliers should not steer training.</p> <p>Supervised learning requires a significant amount of labeled data for the model to learn effectively.</p> <p>The classic example is a model trained on thousands of cat and dog images that learns to classify new photos into one of these two categories. Classification models can learn to distinguish any number of classes, as long as they have a representative dataset.</p> <p>Generally, classification models predict the probability of an input sample belonging to each output class. These probabilities can be interpreted as the model’s confidence that a particular sample belongs to each class. A predicted probability can be converted to a class value by selecting the label with the highest probability.</p> <h3 id="example-1">Example</h3> <p>A model trained to distinguish cat and dog images, when given a cat image as input, might output the following:</p> <table> <thead> <tr> <th>Class</th> <th style="text-align: right">Predicted probability</th> </tr> </thead> <tbody> <tr> <td>Cat</td> <td style="text-align: right">0.876</td> </tr> <tr> <td>Dog</td> <td style="text-align: right">0.124</td> </tr> </tbody> </table> <p>The model decides the image is a cat because that probability is highest. We can also think of this value as the model’s confidence in its conclusion.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">probabilities</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">predict_proba</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>  <span class="c1"># [[0.876, 0.124], ...]
</span><span class="n">predictions</span> <span class="o">=</span> <span class="n">probabilities</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># pick the most likely class
</span></code></pre></div></div> <p>Two cautions about reading those numbers. First, confidence is not correctness: a model can be wrong at 0.99 just as easily as at 0.51, and models trained without calibration are often systematically overconfident. Second, <code class="language-plaintext highlighter-rouge">argmax</code> is only one decision rule. For a medical screening test you might act on any probability above 0.2, because a missed case costs far more than a false alarm.</p> <blockquote class="block-warning"> <p><strong>Warning</strong></p> <p>Accuracy alone hides failure. If 99% of credit card transactions are legitimate, a model that labels every transaction “legitimate” scores 99% accuracy and catches zero fraud. For imbalanced problems, read precision, recall, and the confusion matrix instead.</p> </blockquote> <hr/> <h2 id="unsupervised-learning">Unsupervised Learning</h2> <p>What if we do not have labeled data, or our goal is to discover structure we do not already know about? This is where unsupervised learning comes in, because the input consists of raw data without labels.</p> <p>Three tasks cover most practical uses.</p> <h3 id="clustering">Clustering</h3> <p>Clustering groups samples that resemble each other, without being told what the groups should be. Customer segmentation is the standard example: you do not know in advance that there are four kinds of shoppers, so you ask the data.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.cluster</span> <span class="kn">import</span> <span class="n">KMeans</span>

<span class="n">clusters</span> <span class="o">=</span> <span class="nc">KMeans</span><span class="p">(</span><span class="n">n_clusters</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">0</span><span class="p">).</span><span class="nf">fit_predict</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
</code></pre></div></div> <p>The catch is visible right there in the code. The number 4 was a decision, not a discovery, and nothing in the output tells you it was the right one.</p> <h3 id="association-rule-learning">Association Rule Learning</h3> <p>Imagine a supermarket sales dataset containing all purchases made by customers, basket by basket. By grouping frequently purchased items together, we can extract product relationships and choose to place products on nearby shelves or offer discounts on one product when others are purchased.</p> <h3 id="dimensionality-reduction">Dimensionality Reduction</h3> <p>Another use of this type of learning is <strong>dimensionality reduction</strong>. The aim is to reduce the number of features while preserving the value of the data. This is especially useful when the dataset is very large and sparse.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.decomposition</span> <span class="kn">import</span> <span class="n">PCA</span>

<span class="n">reducer</span> <span class="o">=</span> <span class="nc">PCA</span><span class="p">(</span><span class="n">n_components</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>
<span class="n">X_small</span> <span class="o">=</span> <span class="n">reducer</span><span class="p">.</span><span class="nf">fit_transform</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">reducer</span><span class="p">.</span><span class="n">explained_variance_ratio_</span><span class="p">.</span><span class="nf">sum</span><span class="p">())</span>  <span class="c1"># how much signal survived
</span></code></pre></div></div> <p>Reducing dimensions speeds up training, shrinks memory use, and often improves accuracy by removing noise. It also makes data visualizable, which is frequently how you first notice that two of your classes are hopelessly entangled.</p> <p>None of these three are only for unlabeled problems. Unsupervised learning is also used as a complement to supervised learning: it can help explore data, even pre-labeled data, and reveal groupings or patterns that may have gone unnoticed.</p> <blockquote class="block-warning"> <p><strong>Warning</strong></p> <p>There is no test set here. With no labels there is no objective score to optimize, so evaluating unsupervised results depends on judgment, domain knowledge, and downstream usefulness. Treat every cluster count and component count as an assumption you have to defend.</p> </blockquote> <hr/> <h2 id="reinforcement-learning">Reinforcement Learning</h2> <p>Reinforcement learning is different from supervised and unsupervised learning. In this context, the learning system is called an <strong>agent</strong>. The agent learns by observing its environment, performing actions, and evaluating them based on a <strong>reward</strong> signal.</p> <pre><code class="language-mermaid">flowchart LR
  A["Agent"] --&gt;|action| E["Environment"]
  E --&gt;|"new state, reward"| A
</code></pre> <p>The agent is designed to improve itself by adjusting action parameters and aiming to achieve a larger reward. The model changes its behavior to optimize for the highest reward. The rule it follows when choosing an action is called its <strong>policy</strong>, and training is the search for a policy that maximizes reward accumulated over time rather than reward collected right now.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="nf">reset</span><span class="p">()</span>

<span class="k">for</span> <span class="n">step</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_steps</span><span class="p">):</span>
    <span class="n">action</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">act</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>  <span class="c1"># mostly exploit, occasionally explore
</span>    <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">done</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
    <span class="n">agent</span><span class="p">.</span><span class="nf">learn</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">next_state</span><span class="p">)</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">next_state</span>
    <span class="k">if</span> <span class="n">done</span><span class="p">:</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="nf">reset</span><span class="p">()</span>
</code></pre></div></div> <p>That single line of commentary, “mostly exploit, occasionally explore”, is one of the central tensions of the field. An agent that only exploits what it already knows never discovers a better strategy. An agent that only explores never uses what it learned.</p> <p>Reinforcement learning is heavily used in robotics. For example, a robot can learn to move around its environment by gradually learning from mistakes. Hitting a wall decreases the reward, while moving without collision increases the reward.</p> <blockquote class="block-danger"> <p><strong>Danger</strong></p> <p>The agent optimizes the reward you actually wrote, not the goal you meant. A cleaning robot rewarded for dirt collected can learn to tip dirt onto the floor so it can collect it again. Reward design, not algorithm choice, is usually the hard part of reinforcement learning.</p> </blockquote> <hr/> <h2 id="learning-modes">Learning Modes</h2> <p>Another important feature of machine learning systems is whether they learn in one-shot mode, often called batch learning, or in a continuously incremental mode, often called online learning.</p> <h3 id="batch-learning">Batch Learning</h3> <p>In batch learning, also called offline learning, the system is trained using all available data. This is usually a long and computationally expensive process, so it is only performed occasionally. When you want to retrain the model, you train it again on all the data, usually combining old data with any meaningful new data.</p> <p>Fortunately, this training method can be automated. For example, a team might choose to retrain a model every night or every week.</p> <h3 id="online-learning">Online Learning</h3> <p>If you need a system that responds quickly to change, such as fraud detection or cyber attack detection, online learning can be a better solution. In online learning, the system is trained sequentially by taking small groups of data called <strong>mini-batches</strong> as input. Learning from new data is cheap and fast because the system updates as data arrives.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">SGDClassifier</span>

<span class="n">model</span> <span class="o">=</span> <span class="nc">SGDClassifier</span><span class="p">()</span>

<span class="k">for</span> <span class="n">X_batch</span><span class="p">,</span> <span class="n">y_batch</span> <span class="ow">in</span> <span class="nf">stream_of_minibatches</span><span class="p">():</span>
    <span class="n">model</span><span class="p">.</span><span class="nf">partial_fit</span><span class="p">(</span><span class="n">X_batch</span><span class="p">,</span> <span class="n">y_batch</span><span class="p">,</span> <span class="n">classes</span><span class="o">=</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">])</span>
</code></pre></div></div> <p>Online learning is useful when you need reactive system behavior or when you have limited computational power. The term “online” does not necessarily mean the system is connected to a network. It means the system learns from a continuous stream of data.</p> <h3 id="choosing-between-them">Choosing Between Them</h3> <table> <thead> <tr> <th> </th> <th>Batch (offline)</th> <th>Online (incremental)</th> </tr> </thead> <tbody> <tr> <td>Training data</td> <td>All of it, at once</td> <td>Small mini-batches, continuously</td> </tr> <tr> <td>Cost per update</td> <td>High, so updates are rare</td> <td>Low, so updates are constant</td> </tr> <tr> <td>Reaction to change</td> <td>Slow, bounded by retraining cadence</td> <td>Fast, within minutes</td> </tr> <tr> <td>Infrastructure</td> <td>A scheduled pipeline</td> <td>A streaming pipeline plus live monitoring</td> </tr> <tr> <td>Main risk</td> <td>The model quietly goes stale</td> <td>Bad data corrupts the model quickly</td> </tr> </tbody> </table> <p>An important parameter in online learning is the <strong>learning rate</strong>: how strongly the model reacts to each new mini-batch. Set it high and the system adapts fast but forgets what it learned before. Set it low and it is stable but sluggish.</p> <blockquote class="block-warning"> <p><strong>Warning</strong></p> <p>An online model has no natural checkpoint to fall back to. If corrupted or adversarial data enters the stream, performance degrades continuously and often silently. Production online systems need input validation, live performance monitoring, and the ability to roll back to a saved snapshot.</p> </blockquote> <hr/> <h2 id="inference-types">Inference Types</h2> <p>One last way to categorize machine learning systems is by how they generalize.</p> <h3 id="model-based-systems">Model-Based Systems</h3> <p>Model-based systems aim to create a representation, or model, of knowledge. That model is then used to produce outputs.</p> <h3 id="instance-based-systems">Instance-Based Systems</h3> <p>Instance-based systems do not generalize from an unseen input in the same way. Instead, they compare the input with previous data stored in memory and try to find the closest match.</p> <h3 id="example-scenario">Example Scenario</h3> <p>Given a set of coordinates, such as X and Y values, suppose we want to estimate the right Y value for a new X value. Take four known points:</p> <table> <thead> <tr> <th>x</th> <th style="text-align: right">y</th> </tr> </thead> <tbody> <tr> <td>1</td> <td style="text-align: right">2.1</td> </tr> <tr> <td>2</td> <td style="text-align: right">3.9</td> </tr> <tr> <td>3</td> <td style="text-align: right">6.2</td> </tr> <tr> <td>4</td> <td style="text-align: right">7.8</td> </tr> </tbody> </table> <p>and ask for a prediction at \(x = 3.4\).</p> <p>One strategy is to compare the new X value with known X values, take the closest known point, and assign its Y value to the new point. The nearest known x is 3, so the prediction is <strong>6.2</strong>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.neighbors</span> <span class="kn">import</span> <span class="n">KNeighborsRegressor</span>

<span class="n">model</span> <span class="o">=</span> <span class="nc">KNeighborsRegressor</span><span class="p">(</span><span class="n">n_neighbors</span><span class="o">=</span><span class="mi">1</span><span class="p">).</span><span class="nf">fit</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">)</span>
<span class="n">model</span><span class="p">.</span><span class="nf">predict</span><span class="p">([[</span><span class="mf">3.4</span><span class="p">]])</span>  <span class="c1"># 6.2
</span></code></pre></div></div> <p>This approach is simple, but it relies on the strong assumption that a new point’s output is determined by its nearest neighbor. The system learns its knowledge by memory and applies a similarity measure, such as distance along the X dimension, to new situations.</p> <p>Another approach is to create a representation of how the existing data was generated and use that model to estimate the Y value for a new X value. A model is a set of parameters that, when tuned appropriately, can provide a reasonable estimate for unseen inputs.</p> <p>In a simple example, the model might be a straight line that best represents a series of points:</p> \[\hat{y} = wx + b\] <p>The parameters are the slope \(w\) of the line and the point \(b\) where it intersects the y-axis. Training the model means finding good values for these parameters. Fitting the four points above by least squares gives \(w = 1.94\) and \(b = 0.15\), so the prediction at \(x = 3.4\) is <strong>6.75</strong>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LinearRegression</span>

<span class="n">model</span> <span class="o">=</span> <span class="nc">LinearRegression</span><span class="p">().</span><span class="nf">fit</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">)</span>
<span class="n">model</span><span class="p">.</span><span class="nf">predict</span><span class="p">([[</span><span class="mf">3.4</span><span class="p">]])</span>  <span class="c1"># 6.75
</span></code></pre></div></div> <p>Two different answers, 6.2 and 6.75, from the same four points. Neither is wrong. They encode different beliefs about the data: that nearby points behave alike, or that the underlying relationship is a line.</p> <table> <thead> <tr> <th> </th> <th>Instance-based</th> <th>Model-based</th> </tr> </thead> <tbody> <tr> <td>What is stored</td> <td>The training data itself</td> <td>A set of parameters</td> </tr> <tr> <td>Training cost</td> <td>Almost none</td> <td>Can be very high</td> </tr> <tr> <td>Prediction cost</td> <td>Grows with the dataset</td> <td>Constant, regardless of data size</td> </tr> <tr> <td>Adding new data</td> <td>Just append it</td> <td>Usually requires retraining</td> </tr> <tr> <td>Typical example</td> <td>k-nearest neighbors</td> <td>Linear regression, neural networks</td> </tr> </tbody> </table> <p>A machine learning model can contain tens of thousands or even millions of parameters. Training those parameters requires computational power, and improving the model training process is an important research and engineering topic. That cost is also why model-based systems dominate in production: you pay once during training, then every prediction is cheap.</p> <hr/> <h2 id="main-challenges-of-machine-learning">Main Challenges of Machine Learning</h2> <h3 id="insufficient-data">Insufficient Data</h3> <p>The fundamental assumption of machine learning is having enough data to train models and then use them to solve problems. In the real world, there may be situations where the available data is not sufficient to train a model that can identify useful patterns.</p> <p>Even simple problems can require thousands of examples. Complex problems like image recognition or speech recognition may require millions of examples.</p> <p>Various organizations are moving toward open data platforms to share datasets and enable applications that would otherwise be difficult to build.</p> <p>The labeling issue is also important. Services like CloudFactory or AWS Mechanical Turk try to meet this need by bringing together organizations that need data labeling and people who can label that data. Such services have limitations, including labeling accuracy and completion time.</p> <p>When data is genuinely scarce, <strong>transfer learning</strong> is often the practical answer. Instead of training from nothing, you start from a model already trained on a large general dataset and adapt it to your task with a much smaller one. A great deal of applied computer vision and NLP works this way.</p> <h3 id="low-quality-and-non-representative-data">Low-Quality and Non-Representative Data</h3> <p>Another common problem is low-quality training data. Missing, poorly formatted, or incorrect data can be fatal for a machine learning project. Ideally, high-quality data should be produced directly, but many projects start from existing data that is messy or incomplete.</p> <p>Therefore, one of the most important and time-consuming steps in developing an ML application is <strong>data preprocessing</strong>.</p> <p>Data preprocessing consists of cleaning the data and preparing it for the machine learning model. This can include removing damaged samples, adjusting string formats, and managing missing values. The preprocessing stage is context-dependent and can take many forms.</p> <p>At this stage, we may also try to increase the size of the dataset. For example, if we have an image dataset, we might add transformed copies of images, such as rotated or blurred versions. This lets the model learn from different views of the same image.</p> <p>This technique, called <strong>data augmentation</strong>, is useful for increasing model robustness. The model learns to recognize images even when they are damaged or altered. However, augmentation usually does not add truly new information. That can only come from additional data.</p> <p>Non-representative data is another common issue. For a model to generalize effectively, it must have seen cases that cover most realistic situations. When the training set systematically misses part of the population it will face in production, the gap is called <strong>sampling bias</strong>, and no amount of model tuning repairs it.</p> <p>For example, consider a dataset of temperatures collected on various days of the year, where the task is to predict the temperature of a given day. If we only have November temperatures, how can the model discover the April temperature pattern? Even worse, if that particular November was unusually warm, the model may produce misleading predictions.</p> <p>As a thought experiment, a random model that generates a random number within the minimum and maximum temperature range could outperform a model trained on non-representative data.</p> <h3 id="underfitting">Underfitting</h3> <p>Underfitting occurs when the model is too simple to represent the structure of the dataset, so it cannot capture the patterns in the data.</p> <p>For example, if we wanted to use a linear model to classify dog and cat images, we would probably get unacceptable performance because a linear model cannot capture the complexity of image data.</p> <p>One solution to underfitting is to train more complex models, such as neural networks with many parameters. These models can account for more variables that may affect the output. In a 64 by 64 pixel image, there are 4096 possible pixel positions that may influence the result. A model with too few parameters may struggle with this complexity.</p> <p>Underfitting is the easier failure to detect, because it is visible immediately: the model performs badly on the very data it was trained on.</p> <h3 id="overfitting">Overfitting</h3> <p>Overfitting is the opposite problem. It happens when a model is too complex for the task or the amount of data available.</p> <p>For example, a complex high-degree curve trained to predict house prices may represent the training dataset extremely well, but fail on new data. The model has memorized the training data rather than learning a pattern that generalizes.</p> <p>The standard countermeasures are more data, a simpler model, and <strong>regularization</strong>, which adds a penalty for large parameter values so the model prefers smoother solutions:</p> \[L_{\text{total}} = L_{\text{data}} + \lambda \sum_{j} w_j^2\] <p>The coefficient \(\lambda\) controls the trade-off. At zero you are back to plain training. Too high and you force the model into underfitting.</p> <h3 id="data-leakage">Data Leakage</h3> <p>A subtler failure is <strong>data leakage</strong>: information that will not be available at prediction time sneaks into training. Scaling your features before splitting the data, using a column derived from the answer, or including future measurements in a time series all cause it.</p> <p>Leakage is dangerous precisely because it looks like success. Validation scores are excellent, everyone is pleased, and the model collapses the moment it meets real inputs. When results look surprisingly good, that is the time to check the pipeline rather than celebrate.</p> <h3 id="the-balance-between-underfitting-and-overfitting">The Balance Between Underfitting and Overfitting</h3> <p>The balance between model complexity, amount of data, and task difficulty is one of the fundamental concepts behind model architecture choices.</p> <ul> <li><strong>Underfitting</strong>: The model is too simple and cannot capture the complexity of the dataset.</li> <li><strong>Normal Learning</strong>: The model captures the general pattern in the data without memorizing each point.</li> <li><strong>Overfitting</strong>: The model adapts too strongly to the training data and has difficulty generalizing later.</li> </ul> <p>To find that balance you need to measure it, which means splitting the data before training: a <strong>training set</strong> to learn parameters, a <strong>validation set</strong> to compare models and tune settings, and a <strong>test set</strong> touched only once, at the end, to estimate real performance. Comparing the first two errors tells you which failure you have:</p> <table> <thead> <tr> <th>Training error</th> <th>Validation error</th> <th>Diagnosis</th> <th>What to try</th> </tr> </thead> <tbody> <tr> <td>High</td> <td>High</td> <td>Underfitting</td> <td>A larger model, better features, longer training</td> </tr> <tr> <td>Low</td> <td>High</td> <td>Overfitting</td> <td>More data, regularization, a simpler model, augmentation</td> </tr> <tr> <td>Low</td> <td>Low</td> <td>Good fit</td> <td>Ship it, then monitor it</td> </tr> <tr> <td>High</td> <td>Low</td> <td>Suspicious</td> <td>Check the split, look for leakage or a bug</td> </tr> </tbody> </table> <p>When data is limited, <strong>cross-validation</strong> gives a more reliable reading than a single split: the training data is divided into k parts, the model is trained k times holding out a different part each round, and the scores are averaged.</p> <hr/> <h2 id="putting-it-together">Putting It Together</h2> <p>The three axes and the challenges are not separate lists. They compose into the shape of an actual project.</p> <pre><code class="language-mermaid">flowchart LR
  A["Frame the problem"] --&gt; B["Collect data"]
  B --&gt; C["Preprocess and split"]
  C --&gt; D["Train and validate"]
  D --&gt; E{"Good enough?"}
  E --&gt;|no| C
  E --&gt;|yes| F["Deploy"]
  F --&gt; G["Monitor"]
  G --&gt;|drift| B
</code></pre> <p>Two things about that loop are worth noticing. Most of the effort in a real project sits in the middle, in collecting and preparing data, not in the modeling step that gets the attention. And the loop does not end at deployment: the world changes, incoming data drifts away from what the model was trained on, and performance decays even though nothing in the code changed.</p> <hr/> <h2 id="conclusion">Conclusion</h2> <p>We have seen how machine learning models can be classified, and we have introduced classification, regression, learning modes, inference types, and common ML challenges.</p> <p>If you keep three things from this post, make them these. First, describe a system by its supervision mode, learning mode, and inference type before you reach for an algorithm name. Second, most failures are data failures, not model failures. Third, the gap between training and validation performance is the single most informative number you will look at.</p> <p>In the continuation of this series, we will build the technical foundation needed to create machine learning models. We will start with mathematical concepts and continue by examining the architectures of machine learning models. The ultimate goal is to gain the ability to build AI models for real problems.</p>]]></content><author><name>Selcuk Senturk</name></author><category term="computer-science"/><category term="machine-learning"/><category term="data-science"/><category term="intelligent-systems"/><category term="ml-systems"/><summary type="html"><![CDATA[Understanding the fundamentals of machine learning: types, learning modes, and main challenges.]]></summary></entry></feed>